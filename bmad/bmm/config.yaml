# BMAD Project Configuration
# This file contains project-level settings used by all BMAD agents and workflows

# User Information
user_name: Diego
communication_language: English

# Project Settings
project_name: Zillow Property Search
project_type: web_application
project_level: 1  # 0=atomic change, 1=small feature, 2=medium project, 3=large/complex

# Output Folders
output_folder: bmad/bmm
docs_folder: ${output_folder}/docs
stories_folder: ${output_folder}/stories
epics_folder: ${output_folder}/epics

# Workflow Settings
current_phase: 1-discovery  # 1-discovery, 2-planning, 3-solutioning, 4-implementation
workflow_status_file: ${output_folder}/workflow-status.yaml

# Technology Stack (customize as needed)
tech_stack:
  frontend: Next.js, React, TypeScript
  backend: Node.js
  database: TBD
  deployment: TBD
